const fs = require('fs');

async function testInsert() {
    // I can't easily authenticate without the secrets.
    // Instead I will just read the log output or hit the local /api/salesforce/invoice endpoint!
}
testInsert();
